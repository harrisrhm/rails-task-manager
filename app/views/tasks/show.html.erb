<div class="d-flex justify-content-between">
    <h1><%= @task.title %></h1>
    <div class="btn btn-primary"><%= link_to 'Edit this task', edit_task_path %></div>
</div>
<p><%= @task.details %></p>
<p><%= @task.completed ? icon('far', 'check-square') : icon('far', 'square') %> <%= @completed %> </p>

<%= link_to "Back to list", tasks_path %>